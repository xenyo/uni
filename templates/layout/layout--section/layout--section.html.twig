<div{{ attributes }}>
  {% if content.main %}
  {% if layout.id == 'wrapped' %}<div{{ wrapper_attributes }}>{% endif %}
    <div{{ region_attributes.main }}>
      {{ content.main }}
    </div>
  {% if layout.id == 'wrapped' %}</div>{% endif %}
  {% endif %}
  {{ styles }}
</div>
