uni_default:
  label: 1. Default
  category: Uni
  template: templates/layout/layout--uni-default
  regions:
    main:
      label: Main content
  icon_map:
    - [padding, main, main, main, main, padding]
